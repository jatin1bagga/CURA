from setuptools import find_packages, setup

setup(
    name="CURA medibot",
    version="0.1.0",
    author="JATIN BAGGA",
    author_email="aloc1345@gmail.com",
    packages=find_packages(),#tells Python which folders contain code (like your src/ folder).
    install_requires=[]# the libraries to be installed try to install
)
